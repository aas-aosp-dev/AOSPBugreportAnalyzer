# Core Parser

Abstractions and utilities for turning raw Android bugreport artifacts into structured domain objects. Contains entry points for log segmentation, parsers, and future pipeline stages to extract device state snapshots.
